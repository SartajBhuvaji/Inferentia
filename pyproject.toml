[project]
name = "inferentia"
version = "0.3.0"
description = "Building an LLM Inference Engine"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.2.0",
    "pytest>=9.0.3",
    "torch>=2.11.0",
    "transformers>=5.5.4",
]
